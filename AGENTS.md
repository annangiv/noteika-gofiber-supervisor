# Noteika — agent context

Read **`docs/PRODUCT.md`** and **`docs/ROADMAP.md`** before building features.

**Tagline:** Notes that resurface before you duplicate yourself.

**v1 job:** Save anything once (paste + optional project), find later by meaning.

**Not v1:** Graph view, team sharing, in-app AI chat, folder hierarchies, type picker on save.

**Stack:** Rust/Actix (`src/`), Stoolap (`migrations/`), React (`frontend/react/`).
