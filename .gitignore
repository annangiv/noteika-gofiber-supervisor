# Database (BadgerDB runtime data — never commit)
data/
data-test/
*.vlog
*.mem

# Environment files
.env

# Go binaries
server
go-fiber-supervisor

# Frontend build artifacts (built in Docker)
frontend/node_modules/

# Capacitor native projects (generated locally)
frontend/ios/
frontend/android/
